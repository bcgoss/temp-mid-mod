<div class='link' data-id="<%=link.id%>" id="link-<%=link.id%>">
  <p class='link-title' contenteditable=true><%=link.title%></p>
  <p class='link-url' contenteditable=true><%=link.url%></p>
  <p class='link_read'><%= link.read_status %></p>
  <p class='link_buttons'>
    <button class='mark-as-read'><%= link.button_status %></button>
    <%= button_to 'Edit', edit_link_path(link), method: 'get', class: 'button'%>
  </p>
</div>
